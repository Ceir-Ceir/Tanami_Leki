# Shopify App

Reserved for a standalone Shopify CLI project; nothing from the Streamlit app is inside yet.

Initialize this folder later with the Shopify CLI (e.g., `shopify app create`) to build the Shopify chatbot without refactoring the existing Streamlit setup.
